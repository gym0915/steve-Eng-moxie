---
name: steve-Eng-moxie
description: Use when the user mentions “英语默写”, “英语单词默写”, “英语词组默写”, “英语句子默写”, “默写英语”, “默写英语单词”, “默写英语词组”, “默写英语句子”, “默写英文”, “默写英文单词”, “默写英文词组”, “默写英文句子”, “English dictation”, “English vocabulary dictation”, “English phrase dictation”, or “English sentence dictation”, or asks to use provided images or text to create a printable Word worksheet for looking at Chinese prompts and writing the corresponding English words, phrases, or sentences.
---

# 英语默写

## 核心原则

只使用用户资料中实际出现的中文释义，严格保持原顺序，依照 `assets/template.html` 制作含信息栏、纯中文提示和统一书写横线的 `.docx` 文档。词条提示中不得夹带词性、编号、英文、音标或其他元数据。

**REQUIRED SUB-SKILL:** Use `documents:documents` to create and visually verify the Word document. Read its `SKILL.md` completely before document work.

## 工作流程

1. 收集用户本次提供的全部图片或文字资料。资料不足时，只询问缺失资料，不虚构内容。
2. 处理图片时，先判断人类正常阅读方向；必要时旋转后再识别。只读取清晰可见内容，无法确认的内容标记为待确认，不猜测。
3. 提取英语单词、英语词组及其对应中文释义；最终只保留中文释义。删除栏目标题、英语答案、音标、编号、项目符号，以及 `n.`、`v.`、`adj.`、`adv.`、`prep.`、`pron.`、`conj.`、`num.`、`art.`、`aux.`、`modal v.` 等所有英文词性标记。无论这些标记位于中文释义之前、之后或同一行，都不得进入文档。
4. 严格保持顺序：
   - 图片：逐行读取，同一行从左到右，再读下一行；左右两侧都有词条时，先左后右。
   - 文字：按原文出现顺序。
   - 不合并词条，不改写中文释义，不补充未显示内容。
5. 将提取结果保存为 UTF-8 JSON：`{"items":[{"prompt":"星期一","answer":"Monday"}]}`。`answer` 只用于校验，绝不写入学生文档。
6. 使用工作区依赖加载器提供的 Python 运行时执行 `scripts/create_dictation_doc.py INPUT_JSON OUTPUT_DOCX`。`assets/template.html` 是视觉权威：A4 纵向、姓名／班级／日期／得分信息栏、红色章节标记、“看中文写英文”及题目总数、每行 5 题。
7. 每个词条只排成两行：纯中文释义、统一长度横线 `________`。中文释义内部原有的中文标点和含义保持不变；不得出现任何英文字母、英语缩写、音标、词性、序号、项目符号或说明文字。
8. 创建 Word 文档：A4、纵向、每页 5 列；按顺序逐行从左到右连续排列；内容区域无可见边框；为手写留出适当空间，同时保持紧凑清晰。内容较多时自然分页，并重复模板信息栏和章节标题。
9. 渲染并目视检查所有页面；发现截断、溢出、空白页、可见边框、顺序错乱、列数异常或中文显示为方框时，修正并重新检查。
10. 将最终 `.docx` 保存到用户工作区并提供可点击文件链接。

## 排版约束

- 页面：A4，纵向。
- 栏数：每页 5 列。
- 模板：`assets/template.html`，不得绕过模板另起视觉规范。
- 页首：“姓名／班级／日期／得分”填写栏，横向居中；显示“看中文写英文”和准确题数。
- 单元格：仅纯中文提示和 `________`，横线长度全部一致。像 `n. 星期三` 必须清洗为 `星期三`。
- 边框：无边框或透明边框，不得显示方格。
- 内容：不显示英语、英文字母、英文词性缩写、音标、分段标题、编号、项目符号或额外说明；提示部分只显示中文释义。
- 顺序：跨行、跨列、跨页时仍保持提取顺序。

## 交付检查

- [ ] 图片方向正确，必要旋转已完成。
- [ ] 只使用原资料内容，疑难内容未猜测。
- [ ] 分段标题已删除。
- [ ] 词条顺序符合逐行从左到右规则或原文顺序。
- [ ] 姓名、班级、日期、得分栏位位于文档开头；“看中文写英文”和题数与模板一致。
- [ ] 未显示英语答案和音标。
- [ ] 每项只有纯中文释义和统一横线；已逐项确认不存在 `n.`、`v.`、`adj.` 等词性标记、英文字母、编号、项目符号或额外说明。
- [ ] Word 为 A4 纵向、每页 5 列、无可见表格边框。
- [ ] 中文提示及姓名、班级、日期、得分栏在渲染页中清晰可读，未显示为方框或乱码。
- [ ] 全部页面已渲染检查，适合直接打印。

不得在未完成上述检查时宣称文档已完成。
