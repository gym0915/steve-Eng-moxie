---
name: steve-Eng-moxie
description: Use when the user mentions “英语默写”, “英语单词默写”, “英语词组默写”, “英语句子默写”, “默写英语”, “默写英语单词”, “默写英语词组”, “默写英语句子”, “默写英文”, “默写英文单词”, “默写英文词组”, “默写英文句子”, “English dictation”, “English vocabulary dictation”, “English phrase dictation”, or “English sentence dictation”, or asks to use provided images or text to create a printable Word worksheet for looking at Chinese prompts and writing the corresponding English words, phrases, or sentences.
---

# 英语默写

## 核心原则

只使用用户资料中实际出现的词条，严格保持原顺序，制作含居中填写栏、中文提示和统一书写横线的 `.docx` 文档。

**REQUIRED SUB-SKILL:** Use `documents:documents` to create and visually verify the Word document. Read its `SKILL.md` completely before document work.

## 工作流程

1. 收集用户本次提供的全部图片或文字资料。资料不足时，只询问缺失资料，不虚构内容。
2. 处理图片时，先判断人类正常阅读方向；必要时旋转后再识别。只读取清晰可见内容，无法确认的内容标记为待确认，不猜测。
3. 提取英语单词、英语词组及其对应中文释义；删除栏目标题、音标和英语答案。
4. 严格保持顺序：
   - 图片：逐行读取，同一行从左到右，再读下一行；左右两侧都有词条时，先左后右。
   - 文字：按原文出现顺序。
   - 不合并词条，不改写中文释义，不补充未显示内容。
5. 在首个词条前加入居中页首：第一行固定为“姓名：________  学号：________  日期：________”。除非用户明确要求，不额外显示“小标题”字样或课程题头。页首不显示英语答案或音标。
6. 每个词条只排成两行：原始中文释义、统一长度横线 `________`。
7. 创建 Word 文档：A4、纵向、每页 5 列；按顺序连续排列；用无可见边框的表格排版；页首与所有词条横向居中对齐；为手写留出适当空间，同时保持紧凑清晰。
8. 渲染并目视检查所有页面；发现截断、溢出、空白页、可见边框、顺序错乱、列数异常或中文显示为方框时，修正并重新检查。若当前 Word 渲染环境无法加载中文字体，可将原始中文提示和填写栏生成为清晰内嵌图形后再排版，文字内容不得改变。
9. 将最终 `.docx` 保存到用户工作区并提供可点击文件链接。

## 排版约束

- 页面：A4，纵向。
- 栏数：每页 5 列。
- 页首：“姓名／学号／日期”填写栏，横向居中。
- 单元格：仅中文提示和 `________`，横线长度全部一致。
- 边框：无边框或透明边框，不得显示方格。
- 内容：不显示英语、音标、分段标题、编号或额外说明。
- 顺序：跨行、跨列、跨页时仍保持提取顺序。

## 交付检查

- [ ] 图片方向正确，必要旋转已完成。
- [ ] 只使用原资料内容，疑难内容未猜测。
- [ ] 分段标题已删除。
- [ ] 词条顺序符合逐行从左到右规则或原文顺序。
- [ ] 姓名、学号、日期栏位位于文档开头且横向居中；未添加未要求的课程题头或“小标题”文字。
- [ ] 未显示英语答案和音标。
- [ ] 每项只有原始中文释义和统一横线。
- [ ] Word 为 A4 纵向、每页 5 列、无可见表格边框。
- [ ] 中文提示及姓名、学号、日期栏在渲染页中清晰可读，未显示为方框或乱码。
- [ ] 全部页面已渲染检查，适合直接打印。

不得在未完成上述检查时宣称文档已完成。
